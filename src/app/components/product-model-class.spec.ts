import { ProductModelClass } from './product-model-class';

describe('ProductModelClass', () => {
  it('should create an instance', () => {
    expect(new ProductModelClass()).toBeTruthy();
  });
});
